#include <stdio.h>

int main() {
	
	/*
	int a, b;
	scanf("%d%d", &a, &b);

	int p = a > b;

	// true : 1, false : 0 -> p
	
	int q = a < b;
	int r = a ==b; // == : same

	printf("%d\n", p);
	printf("%d\n", q);
	printf("%d\n", r);

	// bool : truth value save variable (1bite)

	*/

	int a, b;
        scanf("%d%d", &a, &b);

        bool p = a > b;

        // true : 1, false : 0 -> p

        bool q = a < b;
        bool r = a ==b; // == : same

        printf("%d\n", p);
        printf("%d\n", q);
        printf("%d\n", r);

	// a >= b : a is same or larger than b
	// a <= b : a is same or smaller than b
	// a != b : a is not b

}
